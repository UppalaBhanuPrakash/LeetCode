class Solution {
public:
    int getMinDistance(vector<int>& nums, int target, int start) {
        int n=nums.size(),mi=INT_MAX;
        for(int i=0;i<n;i++){
            if(nums[i]==target){
                mi=min(mi,abs(i-start));
            }
        }
        return mi;
    }
};
