/**
 * @param {string} s
 * @return {boolean}
 */
var checkOnesSegment = function(s) {
    let c=0;
    for(let i=0;i<s.length-1;i++){
        if(s[i]!=s[i+1])c++;
        if(c>=2)
        {
            return false;
        }
    }
    return true;
};
