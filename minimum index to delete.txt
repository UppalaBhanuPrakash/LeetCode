/**
 * @param {string} s
 * @return {number}
 */
var minimumDeletions = function(s) {
    let res = 0;
    let b = 0;

    for (const c of s) {
        if (c === 'b')
            b++;
        else if (b) {
            res++;
            b--;
        }
    }

    return res;
};